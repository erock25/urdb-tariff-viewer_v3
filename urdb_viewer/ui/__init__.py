"""
UI-layer utilities for Streamlit integration.

This package intentionally contains Streamlit-coupled code (session state, caching,
rendering helpers) so that core models/services can remain framework-agnostic.
"""
